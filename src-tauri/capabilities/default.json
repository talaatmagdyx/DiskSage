{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the main DiskSage window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

